# Plan: CI GREEN — Fix 7 + Verifikation

## Root Cause (verifiziert mit DB-Beweis)

**16 Fehler (15 ERRORS + 1 FAILED) = 1 Root Cause = 1 Fix-Zeile.**

`transcription_tasks.py:880` — `.scalar_one_or_none()` auf Consent-Query nach `client_id` findet 2 Rows (2 Users × gleicher Client × C2_VOICE) → `MultipleResultsFound`.

**DB-Beweis:**
```
user_id=test-user-id,     consent_type=C2_VOICE, consented=True
user_id=dg-test-user-id,  consent_type=C2_VOICE, consented=True
```

## Fix

**Datei:** `backend/app/tasks/transcription_tasks.py:880`
**Änderung:** `.scalar_one_or_none()` → `.scalars().first()`

```python
# VORHER (Zeile 880):
    ).scalar_one_or_none()

# NACHHER:
    ).scalars().first()
```

**Begründung:** Query prüft "EXISTIERT ein C2_VOICE Consent für diesen Client?" → `.scalars().first()` gibt erste Row oder None. Korrekt für EXISTENCE-Gate.

## Verifikation

1. E2E neu laufen lassen → 16 Errors/1 Failure weg
2. 349+ passed (alle vorher passing bleiben passing)
3. 0 FAILED, 0 ERRORS
4. Commit + Push → CI-Verifikation
