# ISO 27001:2022 Compliance for Meeting Automation System

This document outlines the measures, practices, and roadmap implemented in the Meeting Automation System to ensure compliance with ISO 27001:2022 standards.

## 1. Information Security Management System (ISMS)

The system adopts a risk-based approach to information security, establishing controls to protect the confidentiality, integrity, and availability of information assets. It follows the updated Annex A controls of ISO 27001:2022.

## 2. Key Security Controls Implemented (Kategorie 1)

These controls are actively implemented in the codebase:

### 2.1. Authentifizierung und Anwendungssicherheit (A.5.17 / A.8.26)
- **JWT-Auth & RBAC**: Vollständige JWT-basierte Authentifizierung in FastAPI (inkl. rollenbasierter Zugriffskontrolle für DG, Manager, Participant).
- **Session-Blacklisting**: Implementierung einer Redis-basierten Token-Blacklist für sicheren Logout (verhindert Replay-Angriffe).
- **Input-Validierung**: Strikte Input-Validierung für jeden API-Call durch FastAPI und Pydantic.

### 2.2. Datensicherheit & Kryptographie (A.8.24 / A.10)
- **Verschlüsselung at Rest**: SQLAlchemy `EncryptedType` (basierend auf `cryptography`) verschlüsselt sensible Inhalte (PV-Daten, MFA-Secrets) auf Datenbankebene.
- **Passwort-Hashing**: Sicheres Hashing von Passwörtern via bcrypt.

### 2.3. Audit & Logging (ISO 27035 / A.12.4.1)
- **AuditMiddleware**: Speichert alle sicherheitsrelevanten Anfragen (POST/PUT/DELETE) inklusive Benutzer-ID, IP und Aktion in einer dedizierten `audit_logs` Tabelle.
- **Session-Isolierung**: Audit-Logging verwendet dedizierte `AsyncSessionLocal()` Session — unabhängig von FastAPI's Request-Lifecycle (verhindert DB Pool Exhaustion).
- **Fehlerbehandlung**: `db.rollback()` im except-Block verhindert "Session als FAILED markiert".
- **Sicheres Logging**: Anonyme oder veraltete Sessions werden als Metadaten (`stale_user_id`) erfasst, ohne die DB-Integrität zu stören.

### 2.5. Celery Tenant Isolation (A.5.15 / A.8.26) — FIX 2026-06-05
- **Problem**: `_process_recording_pipeline()` und der Exception-Handler queryten `Recording` nur nach `Recording.id`, ohne `client_id` Filter. Ein Angreifer mit direktem Celery-Worker-Zugang könnte Tasks für beliebige Tenants ausführen.
- **Fix**: `client_id` Parameter zur Pipeline-Funktion hinzugefügt. Alle Queries filtern jetzt nach `Recording.id AND Recording.client_id`. API-Caller (`recording_service.py`, `transcriptions.py`) übergeben `client_id` an `process_recording.delay()`.
- **Tests**: 3 Security-Tests (vorher `xfail`) bestehen jetzt. Test-Dokumentation wurde aktualisiert.
- **Status**: ISO 27001 Defense-in-Depth Maßnahme implementiert.

### 2.4. Informationsweitergabe (A.5.14)
- **n8n Webhook Security**: Das Backend verlangt von n8n den Header `X-Internal-API-Key` für Datenabrufe, um unautorisierte Zugriffe zu blockieren.

## 3. Roadmap für Produktions-Reife (Kategorie 2)

Basierend auf einem Security-Review wurden folgende Lücken gegenüber den Best Practices für Cloud-Architekturen identifiziert. Diese werden vor dem Go-Live geschlossen:

### Sofort (Phase 1)
- ✅ **Secret Management (A.8.24)**: K8s Secrets für GLADIA, MISTRAL, LIVEKIT API Keys. ConfigMap für non-sensitive Config. ISO 27001 konform.

### Kurzfristig
- ✅ **Netzwerksegmentierung (A.8.20)**: 7 NetworkPolicies deployt in Staging: `default-deny-all` + service-specific Rules für Postgres, Redis, RabbitMQ, MinIO, Backend, n8n. Labels an Staging-Pod-Namen angepasst.

### Parallel
- **API Gateway & WAF (A.8.21)**: Traefik läuft als Ingress, aber ohne:
  - Rate Limiting
  - DDoS-Schutz
  - Bot-Management
  - Geo-IP Filterung

### Vor Go-Live
- **SSL/TLS in Transit (A.10)**: Derzeit nur HTTP. Benötigt:
  - TLS-Terminator (Let's Encrypt / Traefik TLS)
  - X-Forwarded-Proto Header für Backend
  - LiveKit WSS (WebSocket Secure) aktivieren
- **Session Management Erweiterung**: Implementierung von Session-Fixation Protection und automatischer Terminierung bei Inaktivität.
- **Vulnerability Scanning (A.12.6.1)**: Automatisierte Scans (Trivy) in der CI/CD Pipeline scharfschalten.

## 4. Compliance-Status Staging (2026-06-23)

| ISO Control | Umsetzung | Status |
|-------------|-----------|--------|
| A.8.24 Secret Management | K8s Secrets (GLADIA, MISTRAL, LIVEKIT, DB, MinIO) | ✅ |
| A.8.24 Encryption at Rest | Fernet AES-128 (ENCRYPTION_KEY in ConfigMap) | ✅ |
| A.8.26 Tenant Isolation | client_id Filter in allen DB-Queries | ✅ |
| A.12.4.1 Audit Logging | 118+ Audit-Logs, dedizierte Sessions | ✅ |
| A.5.17 Auth & RBAC | JWT + 5 Rollen (DG, Manager, Participant, System/Tech Admin) | ✅ |
| A.8.20 Network Policies | 7 Policies deployt (default-deny + service-specific) | ✅ |
| A.10 TLS in Transit | Nur HTTP, kein TLS | ❌ |
| A.12.6.1 Vuln Scanning | Kein Trivy in CI/CD | ❌ |
| A.8.21 API Gateway/WAF | Traefik ohne Rate Limiting/WAF | ❌ |
| A.5.14 Informationsweitergabe | n8n Webhook mit X-Internal-API-Key | ✅ |