# Sentinel-Integrationsplan: Staging Verification

**Datum:** 2026-09-18 (aktualisiert 2026-09-19)  
**Cluster:** OCI Staging (`158.180.18.110`, ARM64)  
**Status:** ONNX Fix ✅ bestätigt. Sentinel Fix ❌ 3 Ansätze fehlgeschlagen.

---

## Was bisher getestet wurde

### ✅ ONNX Memory Fix (BESTÄTIGT)

```
Staging Run 9 (Recording 1c91d642):
  Client:      77577f53 (mit C2_VOICE Consent)
  profiles_with_emb: 1 → ONNX LIEF
  Memory Peak: 1973 MiB (vorher: ~5878 MB auf Prod)
  Einsparung:  ~3900 MB (!!!)
  
  → enable_cpu_mem_arena=False FUNKTIONIERT
  → ONNX Reset + malloc_trim FUNKTIONIERT
```

### ❌ Sentinel llama.cpp Fix (3 Ansätze fehlgeschlagen)

```
Run 10 (c1849f2f, Sep 18 19:36, Staging):
  Image:    eb74022d (alle Fixes deployt)
  ONNX:     ✅ AKTIV (158 segs, 272s, Memory 826 MiB)
  Reset:    ✅ ONNX session released before Sentinel load
  Sentinel: 2 chunks (5158 chars) → SIGABRT nach 54s
  Memory:   826 MiB Peak (NICHT OOM!)

  Fix 1: llama-cpp-python 0.3.35 (PR #22327)
    → PR #22327 ist NUR für WebGPU, nicht CPU!
    → SIGABRT IMMER NOCH DA (ops.cpp:5134)
    → ❌ WIRKT NICHT

  Fix 2: KV-Cache Clear (llama_memory_clear)
    → Wurde ausgeführt (logger.debug)
    → SIGABRT TRITT TROTZDEM AUF
    → ❌ WIRKT NICHT

  Fix 3: GGML_NATIVE=OFF
    → Kein Effekt auf SIGABRT
    → ❌ WIRKT NICHT

  Fix 4: n_ctx=1024 → 2048 (reverted)
    → 1024 war zu klein (1144 Tokens > 1024)
    → Kein SIGABRT-Fix
    → ❌ REVERTED
```

---

## Nächste Schritte: Sentinel ARM64 Bug Fix

### Problem (KORREKTE ROOT CAUSE)

```
ggml_compute_forward_set_rows (ops.cpp:5134)
GGML_ASSERT(i1 >= 0 && i1 < ne1) — SIGABRT

Echte Ursache: asyncio.gather + Singleton Llama-Instance
  → 2 Chunks teilen sich 1 Llama (get_sentinel_service() = Singleton)
  → run_in_executor → 2 ThreadPool-Threads rufen self.llm() auf
  → ggml-cpu NICHT thread-safe → Index-Tensor Kollision
  → GGML_ASSERT → SIGABRT (4x, einmal pro OpenMP-Thread)

Code-Stellen:
  transcription_tasks.py:484  → asyncio.gather(*map_tasks)
  sentinel_service.py:161     → run_in_executor(None, lambda: self.llm(...))
  sentinel_service.py:95      → self._semaphore = asyncio.Semaphore(2)

3 Ansätze fehlgeschlagen:
  1. llama-cpp-python 0.3.35 (PR #22327 — NUR WebGPU, nicht CPU)
  2. KV-Cache Clear (llama_memory_clear) — war Nebensache
  3. GGML_NATIVE=OFF — kein Effekt auf Crash
```

### Lösungsoptionen (mit offiziellen Quellen)

```
 EMPFOHLEN ALS ERSTES: OPTION A
  ──────────────────────────────────────
  OPTION A: Semaphore(2) → Semaphore(1)
  → 1 ZEILE Änderung
  → Offiziell: DeepWiki — "Requests are SERIALIZED"
  → Offiziell: llama-cpp-python#1062 — "Request Serialization"
  → Code: sentinel_service.py:95
  → Risiko: Niedrig
  → Erwartung: 100% Success bei ≥2 Chunks
  → WENN FUNKTIONIERT: Problem gelöst
  → WENN NICHT: Option C als Nächstes

  OPTION B: Serialisierung statt asyncio.gather
  → Alternative zu A (macht dasselbe)
  → Code: transcription_tasks.py:484

  OPTION C: Fresh Llama pro Chunk (FALLBACK)
  → Offiziell: llama.cpp#11804 — "different one for each thread"
  → Jeder Chunk bekommt neue Llama-Instanz
  → ~5s Cold-Start pro Chunk
  → Erlaubt weiterhin parallele Verarbeitung

  OPTION D: llama-server (API-Modus)
  → Architektur-Änderung
  → Nur wenn A + C nicht reichen
```

---

## Test-Szenarien

### Test A: Sentinel allein (ONNX Skip)

```
Recording: >5 Minuten
Client: Beliebig (kein Consent nötig)
Erwartung:
  - ONNX: Skip
  - Sentinel: 2+ Chunks
  - Memory: < 2 GiB
  - Pipeline: ✅ Complete
  - KEIN SIGABRT
```

### Test B: ONNX + Sentinel (komplett)

```
Recording: >10 Minuten
Client: Consent-Client (profiles_with_emb ≥ 1)
Erwartung:
  - ONNX: Aktiv (Memory ~360 MB mit Fix)
  - Sentinel: 1+ Chunks
  - Memory: < 3 GiB (vorher: ~5.8 GiB)
  - Pipeline: ✅ Complete
  - KEIN OOM, KEIN SIGABRT
```

### Test C: Mehrere Recordings (Load)

```
3-5 Recordings gleichzeitig
Erwartung:
  - Alle Complete
  - Memory pro Worker < 4 GiB
  - KEIN OOM, KEIN CRASH
```

---

## Erfolgskriterien

| Metrik | Vorher (Prod) | Nachher (Staging) | Status |
|---|---|---|---|
| ONNX Peak (190 segs) | ~5878 MB | < 2000 MiB | ✅ 1973 MiB, 826 MiB |
| Memory Peak (gesamt) | ~5878 MB | < 3000 MiB | ✅ 826 MiB (Run 10) |
| SIGABRT (≥2 Chunks) | 0% Success | 100% Success | ❌ IMMER NOCH DA |
| Pipeline Complete | 50% (4/10) | 100% | ❌ 40% (4/10) |
| OOMKilled | 2x | 0x | ✅ 0x (ONNX Fix) |

---

## Timeline

```
Sofort:   CI/CD Pipeline (n_ctx Revert + Fixes)
15 Min:   Staging Deploy abwarten
30 Min:   Test A — Sentinel allein (≥2 Chunks)
60 Min:   Test B — ONNX + Sentinel (komplett)
90 Min:   Test C — Load Test (3-5 Recordings)
120 Min:  Report + Production Deploy
```

---

**Generated by Codebuff 🤖**  
**Date:** 2026-09-18T14:50:00+02:00  
**Letztes Update:** 2026-09-19T10:15:00+02:00 (Run 10: KV-Cache Fix deployt, SIGABRT immer noch da)
