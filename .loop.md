# MEETING-AUTOMATION LOOP – HARTE REGELN

## LETZTE AKTION
- **Dashboard i18n + KPI-Fixes:**
  1. `t("dashboard.recent_meetings")` → `t("meetings.recent_meetings")` (Key war im falschen Namespace)
  2. `t("dashboard.no_recent_meetings")` → `t("meetings.no_recent_meetings")`
  3. `t("dashboard.ended")` + `t("dashboard.attendees")` — Fehlende Keys in allen 3 JSONs ergänzt (EN/FR/AR)
  4. KPI "Completed Meetings" zeigte `action_stats.completed` (27) statt `meeting_stats.completed` (0) — gefixt
- Frontend rebuilt + deployed (nginx reload)

## STATUS
- ✅ LIVEKIT – Funktioniert (extern via IP:3000)
- ✅ ONLYOFFICE – Funktioniert (Edit PV öffnet Editor + Dokument)
- ✅ NGINX PROXY – Alle Pfade korrekt geproxied (/web-apps/, /cache/, /api/, Regex)
- ✅ DASHBOARD – meeting.status + Redis-Cache-Invalidierung gefixt
- ✅ DASHBOARD I18N – Übersetzungs-Schlüssel korrigiert, KPI-Daten gefixt

## OFFEN (Produktion)
- 🔒 HTTPS – TLS-Terminator + X-Forwarded-Proto Fix (siehe docs/onlyoffice/Nginx-Redirect-Fix.md)
- 🔒 LiveKit WSS – muss auf wss:// umgestellt werden bei HTTPS

---

## ⚠️ REGELN FÜR DEN AGENTEN (NICHT LÖSCHEN)

1. **DU MUSST ETWAS TUN** – nicht nur analysieren, nicht nur vorschlagen
2. **SCHREIBE CODE** – wenn Code fehlt, schreibe ihn
3. **FIXE FEHLER** – wenn Fehler da sind, behebe sie
4. **KEINE AUSREDEN** – "ich könnte", "man sollte" → NEIN
5. **TATSÄCHLICHE ÄNDERUNGEN** – Dateien müssen geändert werden
6. **WENN DU NICHT WEISST, WAS ZU TUN IST** → frage mich, aber mit konkreten Optionen

## 🚨 STOPP-BEDINGUNGEN
- ❌ NICHT: "der Agent sagt, es ist fertig"
- ✅ NUR: Tests bestehen ODER Build erfolgreich ODER Datei geändert
