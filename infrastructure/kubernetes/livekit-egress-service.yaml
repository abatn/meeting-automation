apiVersion: v1
kind: Service
metadata:
  name: livekit-egress
  namespace: meeting-automation
spec:
  selector:
    app: livekit-egress
  ports:
  - name: health
    port: 7000
    targetPort: 7000
    protocol: TCP
  type: ClusterIP
