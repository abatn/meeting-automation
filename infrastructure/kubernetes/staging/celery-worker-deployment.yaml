apiVersion: apps/v1
kind: Deployment
metadata:
  name: celery-worker-staging
  namespace: meeting-automation-staging
  labels:
    app: celery-worker-staging
    worker-tier: gratuit
spec:
  replicas: 2
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: celery-worker-staging
  template:
    metadata:
      labels:
        app: celery-worker-staging
        worker-tier: gratuit
    spec:
      containers:
      - name: celery-worker
        image: docker.io/batnini/meeting-automation-backend:latest
        imagePullPolicy: Always
        command: ["bash", "-c", "until timeout 2 bash -c '</dev/tcp/rabbitmq-staging/5672'; do echo 'Waiting for RabbitMQ...'; sleep 2; done; celery -A app.tasks.celery_app worker --loglevel=info -Q transcription,transcription_gratuit,email,maintenance"]
        resources:
          requests:
            memory: "1500Mi"
            cpu: "200m"
            ephemeral-storage: "200Mi"
          limits:
            memory: "3Gi"
            cpu: "500m"
            ephemeral-storage: "1Gi"
        startupProbe:
          exec:
            command:
            - /bin/sh
            - -c
            - celery -A app.tasks.celery_app inspect ping --timeout=5
          initialDelaySeconds: 10
          periodSeconds: 10
          failureThreshold: 30
        livenessProbe:
          exec:
            command:
            - /bin/sh
            - -c
            - celery -A app.tasks.celery_app inspect ping --timeout=10
          initialDelaySeconds: 60
          periodSeconds: 60
          timeoutSeconds: 30
          failureThreshold: 5
        env:
        - name: PYTHONPATH
          value: "/app"
        envFrom:
        - secretRef:
            name: backend-secrets-staging
        - configMapRef:
            name: backend-config
---
apiVersion: v1
kind: Service
metadata:
  name: celery-worker-staging
  namespace: meeting-automation-staging
  labels:
    app: celery-worker-staging
spec:
  selector:
    app: celery-worker-staging
  ports:
  - port: 5555
    name: stats
  type: ClusterIP
